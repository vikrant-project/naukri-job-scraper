# Usage Examples - Naukri Job Scraper\n\nThis document provides practical examples of using the Naukri Job Scraper.\n\n## Example 1: Search Python Developer Jobs in Delhi\n\n### Using Dashboard\n1. Click \"Start Scraping\"\n2. Fill in:\n   - Keyword: `Python Developer`\n   - Location: `Delhi`\n   - Experience: `Fresher`\n   - Pages: `3`\n3. Click \"Start Scraping\"\n\n### Using Code (scraper.py)\n```python\nfrom scraper import NaukriScraper\n\nscraper = NaukriScraper(headless=True)\nresults = scraper.scrape_jobs(\n    keyword='Python Developer',\n    location='Delhi',\n    experience='Fresher',\n    max_pages=3\n)\n\nprint(f\"Jobs found: {results['jobs_found']}\")\nprint(f\"Jobs saved: {results['jobs_saved']}\")\nprint(f\"Status: {results['status']}\")\n```\n\n## Example 2: Search BCA Fresher Jobs (All India)\n\n### Using Dashboard\n1. Click \"Start Scraping\"\n2. Fill in:\n   - Keyword: `BCA Fresher`\n   - Location: *(leave empty)*\n   - Experience: `Fresher`\n   - Pages: `5`\n3. Click \"Start Scraping\"\n\n### Using Code\n```python\nfrom scraper import NaukriScraper\n\nscraper = NaukriScraper(headless=True)\nresults = scraper.scrape_jobs(\n    keyword='BCA Fresher',\n    location='',  # All India\n    experience='Fresher',\n    max_pages=5\n)\n```\n\n## Example 3: Export Jobs to Excel\n\n### Method 1: Using Dashboard\n1. Go to Dashboard or Jobs page\n2. Click \"Export to Excel\" button\n3. File downloads automatically\n4. Also saved to `exports/` folder\n\n### Method 2: Using Code\n```python\nfrom database import JobDatabase\nimport pandas as pd\nfrom datetime import datetime\n\ndb = JobDatabase()\njobs = db.get_all_jobs()\n\ndf = pd.DataFrame(jobs)\nfilename = f'jobs_{datetime.now().strftime(\"%Y%m%d_%H%M%S\")}.xlsx'\ndf.to_excel(filename, index=False)\nprint(f\"Exported to {filename}\")\n```\n\n## Example 4: Search Jobs with Filters\n\n### Using Dashboard\n1. Go to \"All Jobs\" page\n2. Use search bar: `Python`\n3. Filter by:\n   - Location: `Noida`\n   - Experience: `1-3 years`\n4. Click \"Apply Filters\"\n\n### Using Code\n```python\nfrom database import JobDatabase\n\ndb = JobDatabase()\njobs = db.search_jobs(\n    keyword='Python',\n    location='Noida',\n    experience='1-3'\n)\n\nfor job in jobs:\n    print(f\"{job['title']} at {job['company']}\")\n    print(f\"  Location: {job['location']}\")\n    print(f\"  Salary: {job['salary']}\")\n    print()\n```\n\n## Example 5: Get Database Statistics\n\n```python\nfrom database import JobDatabase\n\ndb = JobDatabase()\nstats = db.get_statistics()\n\nprint(f\"Total Jobs: {stats['total_jobs']}\")\nprint(f\"Jobs Today: {stats['jobs_today']}\")\nprint(f\"Total Companies: {stats['total_companies']}\")\nprint(f\"Total Scrapes: {stats['total_scrapes']}\")\n```\n\n## Example 6: View Scraping History\n\n```python\nfrom database import JobDatabase\n\ndb = JobDatabase()\nhistory = db.get_scraping_history(limit=10)\n\nfor entry in history:\n    print(f\"Keyword: {entry['keyword']}\")\n    print(f\"Location: {entry['location']}\")\n    print(f\"Jobs Found: {entry['jobs_found']}\")\n    print(f\"Date: {entry['scrape_date']}\")\n    print(f\"Status: {entry['status']}\")\n    print(\"-\" * 50)\n```\n\n## Example 7: Add Custom Job Manually\n\n```python\nfrom database import JobDatabase\nimport hashlib\n\ndef generate_job_id(title, company):\n    unique_string = f\"{title}_{company}\".lower().strip()\n    return hashlib.md5(unique_string.encode()).hexdigest()[:16]\n\ndb = JobDatabase()\n\njob_data = {\n    'job_id': generate_job_id('Software Engineer', 'TechCorp'),\n    'title': 'Software Engineer',\n    'company': 'TechCorp',\n    'location': 'Delhi',\n    'salary': '5-8 Lakh',\n    'experience': '2-4 years',\n    'skills': 'Java, Spring Boot, MySQL',\n    'description': 'Exciting opportunity...',\n    'job_url': 'https://example.com'\n}\n\nif db.insert_job(job_data):\n    print(\"Job added successfully!\")\nelse:\n    print(\"Job already exists (duplicate)\")\n```\n\n## Example 8: Scheduled Scraping (Custom)\n\n```python\nfrom apscheduler.schedulers.background import BackgroundScheduler\nfrom scraper import NaukriScraper\nimport time\n\ndef auto_scrape():\n    scraper = NaukriScraper(headless=True)\n    results = scraper.scrape_jobs(\n        keyword='Python Developer',\n        location='Delhi',\n        experience='',\n        max_pages=2\n    )\n    print(f\"Auto-scrape completed: {results['jobs_saved']} jobs saved\")\n\n# Schedule every 4 hours\nscheduler = BackgroundScheduler()\nscheduler.add_job(\n    func=auto_scrape,\n    trigger='interval',\n    hours=4,\n    id='auto_scrape'\n)\n\nscheduler.start()\n\n# Keep running\ntry:\n    while True:\n        time.sleep(60)\nexcept KeyboardInterrupt:\n    scheduler.shutdown()\n```\n\n## Example 9: Delete All Jobs (Reset Database)\n\n```python\nfrom database import JobDatabase\n\ndb = JobDatabase()\n\n# WARNING: This deletes all jobs!\nconfirm = input(\"Delete all jobs? (yes/no): \")\nif confirm.lower() == 'yes':\n    db.delete_all_jobs()\n    print(\"All jobs deleted!\")\nelse:\n    print(\"Cancelled\")\n```\n\n## Example 10: Get Jobs by Salary Range\n\n```python\nfrom database import JobDatabase\n\ndb = JobDatabase()\njobs = db.get_all_jobs()\n\n# Filter high-paying jobs (contains \"Lakh\" in salary)\nhigh_paying = []\nfor job in jobs:\n    salary = job.get('salary', '')\n    if 'Lakh' in salary or 'LPA' in salary:\n        # Try to extract number\n        try:\n            # Simple extraction (e.g., \"5-8 Lakh\")\n            numbers = [int(s) for s in salary.split() if s.isdigit()]\n            if numbers and max(numbers) >= 5:\n                high_paying.append(job)\n        except:\n            pass\n\nprint(f\"Found {len(high_paying)} high-paying jobs (5+ Lakh)\")\nfor job in high_paying[:5]:\n    print(f\"{job['title']} - {job['salary']}\")\n```\n\n## Example 11: Multiple Keyword Search\n\n```python\nfrom scraper import NaukriScraper\nimport time\n\nkeywords = [\n    'Python Developer',\n    'Java Developer',\n    'Data Scientist',\n    'Frontend Developer'\n]\n\nscraper = NaukriScraper(headless=True)\n\nfor keyword in keywords:\n    print(f\"\\nScraping: {keyword}\")\n    results = scraper.scrape_jobs(\n        keyword=keyword,\n        location='Delhi',\n        experience='',\n        max_pages=2\n    )\n    print(f\"  Jobs saved: {results['jobs_saved']}\")\n    time.sleep(30)  # Wait 30 seconds between different searches\n```\n\n## Example 12: Export to CSV Instead of Excel\n\n```python\nfrom database import JobDatabase\nimport pandas as pd\nfrom datetime import datetime\n\ndb = JobDatabase()\njobs = db.get_all_jobs()\n\ndf = pd.DataFrame(jobs)\ncolumns = ['title', 'company', 'location', 'salary', 'experience', 'skills']\ndf = df[columns]\n\nfilename = f'jobs_{datetime.now().strftime(\"%Y%m%d\")}.csv'\ndf.to_csv(filename, index=False)\nprint(f\"Exported to {filename}\")\n```\n\n## Example 13: Get Recently Scraped Jobs (Last 24 Hours)\n\n```python\nfrom database import JobDatabase\nfrom datetime import datetime, timedelta\n\ndb = JobDatabase()\nall_jobs = db.get_all_jobs()\n\n# Filter jobs from last 24 hours\nrecent_jobs = []\nfor job in all_jobs:\n    scraped_date = datetime.fromisoformat(job['scraped_date'].replace('Z', '+00:00'))\n    if scraped_date > datetime.now() - timedelta(days=1):\n        recent_jobs.append(job)\n\nprint(f\"Jobs scraped in last 24 hours: {len(recent_jobs)}\")\n```\n\n## Example 14: Run Scraper with Visible Browser (Debug Mode)\n\n```python\nfrom scraper import NaukriScraper\n\n# Set headless=False to see the browser\nscraper = NaukriScraper(headless=False)\n\nresults = scraper.scrape_jobs(\n    keyword='Python Developer',\n    location='Delhi',\n    experience='Fresher',\n    max_pages=1\n)\n\nprint(results)\n```\n\n---\n\n## Tips & Best Practices\n\n1. **Rate Limiting**: Always use delays between scraping sessions\n2. **Error Handling**: Wrap scraping code in try-except blocks\n3. **Logging**: Check console output for issues\n4. **Database Backups**: Backup `jobs.db` regularly\n5. **Excel Files**: Clean up old exports from `exports/` folder\n6. **CAPTCHA**: If detected, increase delays and reduce pages\n7. **Chrome Updates**: Update Chrome regularly for compatibility\n\n---\n\n## Troubleshooting Commands\n\n### Check if app is running\n```bash\npython test_setup.py\n```\n\n### View database stats\n```python\npython -c \"from database import JobDatabase; db = JobDatabase(); print(db.get_statistics())\"\n```\n\n### Check Python version\n```bash\npython --version\n```\n\n### List installed packages\n```bash\npip list\n```\n\n---\n\nFor more help, see:\n- README.md - Full documentation\n- QUICKSTART.md - Quick setup guide\n- PROJECT_SUMMARY.md - Project details\n